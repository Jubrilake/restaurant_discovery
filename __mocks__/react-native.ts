import * as ReactNative from "react-native";

export const NativeAnimatedHelper = {}; // Mock Animated module

export default Object.setPrototypeOf(
  {
    NativeAnimatedHelper,
  },
  ReactNative
);
